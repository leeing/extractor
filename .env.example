# Document Extractor - 环境变量配置
# 复制此文件为 .env.local 并填入实际值
# 这些值作为默认配置，页面上仍可自定义覆盖

# LLM API Base URL (OpenAI-compatible)
EXTRACT_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# Model ID
EXTRACT_MODEL_ID=qwen-vl-plus

# API Key
EXTRACT_API_KEY=sk-your-api-key-here

# --- 访问控制 (可选) ---
# 设置后所有 API 请求需携带 Authorization: Bearer <token>
# 不设置则跳过鉴权（本地开发无需配置）
# ACCESS_TOKEN=your-shared-secret-here

# --- 限流配置 (可选，0 或不设置表示不限制) ---
# 时间窗口内最大请求数
# EXTRACT_RATE_LIMIT_MAX_REQUESTS=5
# 请求窗口大小（秒）
# EXTRACT_RATE_LIMIT_REQUEST_WINDOW_SECONDS=10
# 每分钟最大输入 Token 数
# EXTRACT_RATE_LIMIT_MAX_INPUT_TPM=30000
# 每分钟最大输出 Token 数
# EXTRACT_RATE_LIMIT_MAX_OUTPUT_TPM=6000
